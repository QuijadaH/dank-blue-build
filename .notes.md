# TO-DO

- Add other config files.
- Convert Hyprland config to Lua.
- Edit `README.md` according to https://github.com/blue-build/template and personal additions.
    - Browser is not pre-installed because I think the `default-flatpaks` always install apps even when you explicitly uninstall them; install preferred browser after install/rebase via Bazaar or Flatpak CLI. 

---

# Notes

## DMS Default Configs for Other Apps (https://github.com/AvengeMedia/DankMaterialShell/tree/master/core/internal/config/embedded)

## DMS Declaratively Enable Plugins

`~/.config/DankMaterialShell/plugin_settings.json`
```
{
    "pluginID": {
        "enabled": true
    }
}
```
> Get `pluginID` from `plugin.json`.
