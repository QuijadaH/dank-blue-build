# TO-DO

- Edit `README.md` according to https://github.com/blue-build/template and personal additions.
    - Browser and media apps are not pre-installed because I'm pretty sure BlueBuild's `default-flatpaks` module always re-install declared apps even when you explicitly uninstall them; install preferred apps after install/rebase via Bazaar or Flatpak CLI.
    - Bazaar, Warehouse, and Flatseal are installed via BlueBuild's `default-flatpaks` module because I think they are core apps for the Flatpak ecosystem.
    - Will convert the Hypland config to LUA once DMS supports it.
- Format DMS Keybind Cheatsheet

---

# Notes

## DMS Default Configs for Other Apps (https://github.com/AvengeMedia/DankMaterialShell/tree/v1.4.6/core/internal/config/embedded)

## DMS Declaratively Enable Plugins

`~/.config/DankMaterialShell/plugin_settings.json`
```
{
    "pluginID": {
        "enabled": true
    }
}
```
> Get `pluginID` from `plugin.json`.
